Excellent feedback — this is exactly the kind of pushback that makes the brief implementation-ready instead of architecture-on-paper. Your team clearly has their hands on the actual codebase, and every one of these points lands. Let me address each, then I'll update the brief.

**URL choice — Option A is correct.** Agreed without reservation. The mixed-pattern concern is real, but the SEO and shareability arguments win decisively. "Tissue repair peptides" is a meaningful query with real volume; `/systems/tissue-repair` is a clean canonical URL that signals "this is the topical authority for this system." Burying it under `/guides/systems/...` dilutes the signal and adds path depth for no benefit. Locking Option A.

**`resolvePrimarySystem()` already exists — that's a meaningful simplification.** Removes the entire data migration concern from Phase 1. The brief should reflect that the function is the source of truth and should be exposed to both profile pages (for breadcrumb computation) and system hub templates (for filtering). No schema changes, no backfill spreadsheet — just plumbing. This also de-risks the project significantly because there's no data drift possible between the two views; they read from the same canonical computation.

**Pathways decoupled from Phase 2.** Right call. Conflating an engineering deliverable with a content deliverable was a planning mistake on my part. Pathways need editorial curation that lives on a different timeline than dev work, and the landing page restructure delivers value on its own. The TODO placeholder approach is clean — ship the section as a hidden/empty state or a collapsed module gated behind a feature flag, and turn it on when the content lands. No blocked deploys.

**System hub as a filtered view of `PEPTIDE_PATHWAYS` — yes, this is the right pattern.** Single source of truth, no duplication, automatic consistency. Any time a peptide profile is added or its system assignment changes, the hub updates with zero additional work. This is also the pattern that makes future multi-system tagging (if it's ever needed) trivial to add — just change the filter predicate.

**Breadcrumbs moved to early Phase 1 — agreed.** I underweighted this. You're right that the BreadcrumbList JSON-LD is the highest-leverage SEO change in the whole project, and it has near-zero implementation cost since `resolvePrimarySystem()` already exists. It should ship with Phase 1, not wait. The reason it matters: every Google-landed peptide profile becomes a topical authority signal pointing up to the system hub. With ~100 profiles and growing, that's 100+ internal links flowing authority into 7–8 hub pages — that's how you rank for the system-level queries.

Let me update the brief with all of this and the cleaner three-task Phase 1 breakdown.The v2 brief is ready to hand off. Key changes captured in the changelog at the bottom so your team can see exactly what shifted from v1.

A few quick observations now that the plan is real:

**The fact that `resolvePrimarySystem()` already exists changes the project's risk profile substantially.** What looked like a meaningful architecture project — new schema, backfill, data migration — is actually three relatively self-contained UI/routing tasks. Phase 1 is realistically a few days of focused work, not weeks. That also means you can ship breadcrumbs and start banking the SEO compound effect *before* the system hubs are even built, if your team wants to parallelize.

**Phase 1 Task 3 (breadcrumbs + footer) is your fastest SEO win.** It can technically ship before Task 1.1 — every profile pointing to a `/systems/{slug}` route that 404s temporarily would be bad, so order matters, but the *implementation* is independent. Once Task 1.1 is deployed even in skeleton form (just the route resolving with placeholder content), Task 1.3 unlocks immediately.

**One thing worth deciding before Task 1.1 starts:** the system metadata config (Quick Breakdown, Deep Dive, descriptions, icon keys, recommended stack IDs, related lab guide slugs). That's content work that has to exist before the template can render anything meaningful. If you want, I can draft a starter version — proposed system list with slugs, names, 1-paragraph descriptions, and which existing peptides each maps to under `resolvePrimarySystem()` logic. That'd give your dev team something to wire against immediately and your content team a starting point to refine. Want me to put that together?