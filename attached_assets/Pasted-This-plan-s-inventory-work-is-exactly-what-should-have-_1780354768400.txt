This plan's inventory work is exactly what should have happened, grounded in the actual code, not assumed, and it surfaced two things worth your attention before you approve.

**First, the good news that changes nothing: Benefits and Affiliate already route.** Replit found `/benefits` and `/affiliate` already exist as routes, so those account rows are already correct, no work needed. That's the inventory paying off, it stopped Replit from rebuilding something that's already done.

**Second, the thing I want you to actually decide on, not wave through: the `/account-settings` vs `/dashboard/settings` overlap.** Replit's plan creates a *new* `/dashboard/settings` with the full inline content (profile, security, notifications, affiliate, danger zone) and *leaves the old `/account-settings` alone* because it "may have external links." Read that carefully, because it means after this task you'll have **two settings pages**: the old profile-only `/account-settings` and the new full `/dashboard/settings`. That's the exact duplication problem this whole project started out to kill, recreated in a different spot.

"Flag it, don't fix it" is a reasonable call to avoid scope creep in this task, but it leaves a live trap: two settings pages that partially overlap, where editing your profile in one might not reflect in the other, and where a stray link could send a user to the stale profile-only version. Before approving, get one answer from Replit: **what actually links to `/account-settings` today, and does it write to the same data as the new page?** If nothing important links to it, the clean move is to redirect `/account-settings` → `/dashboard/settings` so there's one settings page, not two. If something does depend on it, then keeping both is defensible but you need to know they read/write the same underlying data so they don't drift. Don't let "leave it alone" mean "ship two settings pages and hope." That's a five-minute check that prevents a confusing bug.

**Third, a smaller flag on the Academy route.** The plan bundles Research Progress + Batch Verification + Badges + Quiz into `/dashboard/academy`. But Batch Verification (Verify COA) is conceptually a *verification* tool, not an *education* feature, your original Research Tools hub had "Verify COA" as its own distinct card. Folding batch verification into the academy page might bury it. Worth confirming: does Verify COA get its own route (matching its own hub card), or is it living inside academy? If the hub has a "Verify COA" card, that card should land somewhere coherent, not inside the academy page where a user wouldn't think to look. Minor, but it's the kind of mapping that makes navigation feel off.

Everything else is right: routes under `/dashboard/*` matching existing convention, the 16-item no-content-lost checklist, the explicit "don't redesign, don't touch COA work, don't touch the standalone pages" guardrails. The move-don't-rewrite discipline is there.

So before approving #1046: (1) get the `/account-settings` answer, redirect it or confirm shared data, don't ship two settings pages blind, and (2) confirm where Verify COA lands and that it matches its hub card. Both are quick checks, not redesigns.

And the sequencing question from my last message still stands and now matters more: you've got #1045 (COA links in the Orders section) and #1046 (which *moves* the Orders section to `/dashboard/orders`). If COA runs first, its link gets built inline then relocated by 1046. If 1046 runs first, COA builds the link in the already-routed Orders page, nothing moves twice. I'd run **1046 first, then 1045.** Confirm that order before you set either loose, since they both touch the Orders section of dashboard.tsx and doing them backwards is a self-inflicted merge headache.