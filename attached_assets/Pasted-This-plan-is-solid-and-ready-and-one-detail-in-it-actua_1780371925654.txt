This plan is solid and ready, and one detail in it actually validates the concern that prompted the whole task: it found **~60 dangling references**, not the ~28 the TypeScript check first surfaced. So the breakage from Phase 2.5 was more than double what the initial error list showed. That's worth sitting with for a second, because it's the clearest possible evidence that "Phase 2.5 complete, 8/8 walk passed" was wrong by a wide margin. Sixty undefined references in a file that was reported done. The repair plan is right to exist, and the per-name classify-before-fixing approach is correctly the heart of it.

The plan captures everything: classify-don't-blindly-restore as the critical approach, the cross-check against sub-pages to decide dead-vs-live, the `tsc --noEmit` zero-errors gate with actual output shown, the reverse-check on sub-pages, and the re-enumerate-first step so the list is confirmed complete before classifying. Approve it. Three small things to watch, none blocking:

**1. The `History` JSX error (TS2786) is the one genuinely different from the rest, don't let it get lumped in.** Every other item is "name is missing, decide restore-or-delete." But `History used as a JSX component` is a different bug: `History` is being *rendered as a component* (`<History />`) when it's a lucide *icon* that's either not imported or being misused. The fix isn't restore-vs-delete, it's "this JSX is malformed." It could mean the icon import is missing (add it), or it could mean something was meant to be a different component entirely and got mangled in the move. Tell Replit to treat that one as its own diagnosis, not fold it into the classify pass, because "restore the import" might be the wrong fix if the real problem is that the JSX was supposed to reference something else.

**2. "Restore the hook with appropriate query/mutation calls" (Step 4) is where a subtle bug can hide.** For the *live* references that need restoring, declaring a missing mutation means re-creating its query/mutation logic, which endpoint it calls, what it invalidates, etc. If Replit reconstructs a mutation from scratch by guessing, it could wire it to the wrong endpoint or miss a cache invalidation. The safer move: for anything being *restored*, copy the working version from wherever it still exists (a sub-page that has the same mutation, or git history from before the move) rather than re-authoring it. A restored-but-subtly-wrong address-delete mutation is worse than an obvious error. Tell it to source restored logic from existing working copies, not reconstruct from the usage site.

**3. Browser-verify needs real data, same as always.** Step 7 says load `/dashboard` and exercise order history, etc. The order status bar, order number, and tracking link only render meaningfully for an order that *has* that data. Make sure the browser check uses a real order (you have at least one), so "status bar renders" means "renders with real status," not "renders empty and therefore looks fine." Empty-state rendering can mask a broken data path, same trap as the heading-vs-data thing from the Phase 2.5 walk.

So: approve #1069, runs after #1068. Flag the `History` error as a separate diagnosis, restore live references by copying working versions rather than reconstructing, and browser-verify against a real order. The gate is `tsc --noEmit` zero errors with actual output shown, that's the thing that would have caught all 60 of these the first time.

And the through-line, one more time because #1069's very existence proves it: a refactor that moves code between files needs a TypeScript-clean gate, full stop. Phase 2.5 had a route-mount walk and shipped 60 broken references. The walk wasn't wrong, it just can't see this class of bug. `tsc` can. Bake that in as a standing rule for any future move-code-between-files work, the way you've got standing rules for migrations now, and you stop manufacturing #1069-shaped cleanup tasks.

Approve it and run it after #1068. Two show-me gates left: #1068's order-data-and-clean-log, and #1069's zero-`tsc`-errors. Make Replit show you both for real.