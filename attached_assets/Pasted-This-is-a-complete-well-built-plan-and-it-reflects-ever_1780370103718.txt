This is a complete, well-built plan, and it reflects every correction from this whole thread accurately. The migration-collision handling is documented exactly right (leave as-is, verified harmless, with the reasoning baked in), the subscriptions removal correctly touches only the ORM mapping and leaves the Stripe table alone, the two hard gates are explicit, and the out-of-scope fence is tight. I'd approve it. But there are two things in the file references that the plan *contradicts* itself on, and they're worth resolving before you assign, because they change what the hard gates actually prove.

**Look at the file references at the bottom against what the steps claim.** Two of them say the work is *already done*:
- `server/routes.ts:6618-6622 — GET user/subscriptions (already returns [])`
- `server/routes.ts:482-489 — cancel route (already returns 404)`

But Step 4 and the "Done looks like" treat the subscriptions cleanup as work to *do*, and hard gate #2 is "confirm no 500 on `/api/user/subscriptions`." If the endpoint *already returns `[]`*, then either the 500 is already gone (in which case gate #2 is confirming something already true, fine, but know that going in), or the endpoint returns `[]` at the route level while the 500 is happening *deeper* (in a storage/ORM call that the route swallows). The investigation earlier said the 500 came from the ORM querying `WHERE user_id = $1` against the Stripe table. So "the endpoint returns `[]`" and "there's a 500 in the logs" can *both* be true simultaneously, the route catches the ORM error and returns `[]` to the client while Postgres still logged a 500. That means **removing the ORM definition (Step 4) is what actually kills the 500, not the endpoint behavior.** Make sure Replit understands gate #2 is verifying the *log* is clean after the ORM mapping is removed, not just that the endpoint returns `[]` (which it apparently already does). The distinction matters: if it checks "endpoint returns `[]`" and calls the gate passed, it hasn't proven the 500 is gone. It has to check the server log specifically.

**The bigger one: hard gate #1 depends on PayPal sandbox actually working in dev, and that's not guaranteed.** Step 5 says "place a real 2-3 product PayPal sandbox order in dev." Your whole `items` foundation rests on this test. But what if PayPal sandbox isn't wired in dev, or the sandbox flow can't be driven headlessly, or the manual-order path is the only thing that works in dev? The plan offers no fallback. If Replit can't actually place a sandbox order, it'll either get stuck or, worse, "verify" the forward-write path some other way (reading the code, placing a manual order that skips the PayPal capture path) and report the gate passed without actually testing the real checkout write. The forward-write path through *PayPal capture* (`routes.ts:1230-1440, resolvedLineItems`) is specifically the thing that's never been proven, and it's different from the manual-order path. So: confirm PayPal sandbox is functional in dev *before* relying on it for the gate, and if it isn't, the gate needs an explicit alternative (drive the capture path with a mocked PayPal response that still exercises the real `resolvedLineItems` → `items` write). Don't let "place a sandbox order" silently degrade into "place a manual order" that tests a different code path. The gate is only meaningful if it exercises the *actual* checkout write that real customers hit.

Two smaller notes, not blockers:

The UUID regex in Step 3 (`^[0-9a-f-]{36}$`) is fine but slightly loose, it'd match any 36-char hex-and-dash string. Real product IDs presumably are UUIDs so it'll work, but a stricter UUID-format match would avoid accidentally "correcting" a legitimately-named product that happened to fit the pattern. Minor, just have it use a proper UUID shape if product IDs are standard UUIDs.

And Step 1's batchNumber/items decision is correctly placed first (it blocks #1045), but the plan lets Replit *make* the decision by inspecting checkout code. That's a real product decision, single-product-enforced vs. true multi-product, with downstream consequences for COA and cart UX. I'd want *you* to make that call, not have Replit infer it from current code. Have it report what the checkout currently does, then you decide (a) or (b), rather than it deciding and documenting. It's the one step where "investigate and document the decision" should be "investigate, surface to Grayson, then document the decision Grayson makes."

So before assigning: clarify gate #2 checks the *log* not the endpoint return, confirm PayPal sandbox actually works in dev (or define the mock-capture fallback) so gate #1 tests the real write path, and pull the Step 1 model decision up to you rather than letting Replit infer it. Those three keep the two hard gates from passing on technicalities. Everything else is solid and ready.