Update before 5b: I just swapped Replit Secrets from live PayPal credentials back to sandbox. Two things need to happen before we proceed with 5b:
1. Re-run the 5a diagnostic against sandbox. Specifically:

Restart the app to pick up the new env vars
Reload /checkout and capture the new browser console errors
Check the server logs — does /paypal/setup still 401, or does it succeed against sandbox PayPal OAuth now?
Confirm the existing PayPal popup button (in PayPalButton.tsx if I've remembered the file name correctly) still works for a sandbox test purchase, OR if it's also broken now

2. Confirm VITE_PAYPAL_CLIENT_ID strategy. Since we're now in sandbox:

I will set VITE_PAYPAL_CLIENT_ID in Replit Secrets to match the sandbox PAYPAL_CLIENT_ID value
Confirm the frontend code reading import.meta.env.VITE_PAYPAL_CLIENT_ID will see this on next restart
Note that when I go back to live for production, I'll need to update BOTH secrets (PAYPAL_CLIENT_ID and VITE_PAYPAL_CLIENT_ID) to live values

3. Update the 5a findings document. If sandbox behavior is different from live, update the diagnostic so the plan reflects current reality.
Don't start 5b code changes yet. Hand back to me with the updated diagnostic first.


In parallel — your action items
1. Add VITE_PAYPAL_CLIENT_ID to Replit Secrets right now:

Open Replit Secrets (lock icon, left sidebar, or Tools → Secrets)
Click "New Secret"
Key: VITE_PAYPAL_CLIENT_ID
Value: Same as your current sandbox PAYPAL_CLIENT_ID value (copy from that one — they should be identical)
Save
Restart the app

2. Have your sandbox PayPal dashboard ready:

Log into developer.paypal.com/dashboard/accounts to confirm your sandbox business account is set up
This is where you'll view test transactions during Phase 5d testing

3. Bookmark PayPal's sandbox test card numbers:

developer.paypal.com/api/rest/sandbox/card-testing/
Used for Phase 5d testing — different cards trigger different scenarios (success, decline, 3DS challenge, insufficient funds, etc.)